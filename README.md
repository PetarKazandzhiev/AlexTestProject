# AlexTestProject
Testing GitHUub


git add . - "Добавя(стейджва) всички променени файлове към сегашния commit"

git add <FileName> - "Добавя(стейджва) един специфичен файл към  сегашния commit" 

git commit -m "commit message" - "Запазва(скрийншотва) аднатите файлове(промени)"

git checkout <branchName> - "Чеква бранч"
git checkout -b <branchName> - "Чеква и създава бранч"

git push origin <branchName> - "Пушва локалния бранч с всички нови промени към origin"

git pull - "Пулва всички бранчове от origin локално на нашия комптър"
git pull<branchName> - "Пулва един конкретен бранч от origin локално на нашия комптър"

git merge main - "Mergva бранча на който си с main brancha"

git branch - "Дава лист с всички бранчове"

git clone <repositoryLink> - "Клонира репоситори от origin локално на нашия компютър"

git status - "Проверява статуса на определения бранч, в който си."

git log - "Виждаш историята на комитите на бранча"